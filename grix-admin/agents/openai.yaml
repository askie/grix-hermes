interface:
  display_name: "Grix Admin"
  short_description: "Create remote agents and bind local Hermes profiles."
  default_prompt: "Use $grix-admin to create a remote Grix API agent and bind it into a Hermes profile."

policy:
  allow_implicit_invocation: true
