interface:
  display_name: "Grix Admin"
  short_description: "Create remote agents and bind local Hermes profiles."
  default_prompt: "Use $grix-admin to create_grix for existing Grix runtime credentials, manage categories, and bind the result into a Hermes profile."

policy:
  allow_implicit_invocation: true
