# Acceptance Checklist

`grix-egg` 安装成功前，至少确认：

1. 目标内容已经落位
2. 必要的 OpenClaw 配置已经写入并通过 `config validate`
3. 如果创建了新 agent，Grix 绑定已经存在
4. 如果建了测试群，已经保存准确 `session_id`
5. 已向当前私聊单独发送测试群会话卡片
6. 验收群里目标 agent 的身份回答正确
