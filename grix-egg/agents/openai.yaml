interface:
  display_name: "Grix Egg"
  short_description: "Run Hermes agent installation and acceptance workflows."
  default_prompt: "Use $grix-egg to run the Hermes-side Grix install_flow, start the target gateway, and finish acceptance in a test group."

policy:
  allow_implicit_invocation: true
